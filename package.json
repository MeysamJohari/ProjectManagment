{
  "name": "personal-pm",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first personal project management — Markdown + YAML frontmatter on disk.",
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
