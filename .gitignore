# secrets / env
.env

# deps
node_modules/

# build output
dist/
*.tsbuildinfo

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
.vscode/
.idea/
Thumbs.db

# NOTE: data/ is tracked in its OWN local git repo (created by the server
# at startup) for backup purposes. It should NOT be committed into the code
# repo. Uncomment to ignore the data dir from the code repo:
data/
