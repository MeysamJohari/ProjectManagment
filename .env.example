# Copy to .env and adjust before going online.

# ── Server ──
DATA_DIR=./data
PORT=4001
API_TOKEN=dev-token-change-me-before-deploy
ENFORCE_AUTH=false

# ── Client ──
VITE_API_URL=http://localhost:4001
VITE_API_TOKEN=dev-token-change-me-before-deploy
